defmodule TemplateTest do
  use ExUnit.Case

  describe "first_part" do
    test "example" do
      input = """
      """

      assert HistorianHysteria.first_part(input) == 0
    end
  end

  describe "second_part" do
    test "example" do
      input = """
      """

      assert HistorianHysteria.second_part(input) == 0
    end
  end
end
